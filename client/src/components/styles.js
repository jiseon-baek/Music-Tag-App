import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
	ul: {
		justifyContent: 'space-around',
		marginTop: '0rem',
		padding: '6px',
		marginBottom: '1rem',
		
	}
}))